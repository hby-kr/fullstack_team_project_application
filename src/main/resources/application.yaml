spring:
  application:
    name: fullstack_team_project_application
  profiles:
    include: dev_db
  jpa:
    hibernate:
      ddl-auto: update  # 기존 테이블을 수정 가능
    show-sql: true
#  web:
#    resources:
#      static-locations:  ${user.dir}/fullstack_team_project_application/src/main/resources/static/img
#img:
#  upload:
#    path: ${user.dir}/spring_mybatis/src/main/resources/static/img
logging:
  level:
    org.hibernate.orm.jdbc.bind: trace

